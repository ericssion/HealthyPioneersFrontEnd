steps:
  #install node package
  - name: 'ghcr.io/cloud-builders/npm'
    args: ['install']

  #build productive files
  - name: 'ghcr.io/cloud-builders/npm'
    args: ['run','build','--prod']

  #deploy to google cloud app engine
  - name: 'ghcr.io/cloud-builders/npm'
    args: ['app','deploy','--version=prod']

options:
  logging: CLOUD_LOGGING_ONLY
